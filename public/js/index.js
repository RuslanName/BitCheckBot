window.BitCheckAdmin = {
    analytics: window.analytics,
    broadcasts: window.broadcasts,
    config: window.config,
    deals: window.deals,
    login: window.login,
    raffles: window.raffles,
    sidebar: window.sidebar,
    users: window.users,
    utils: window.utils,
    withdrawals: window.withdrawals
};
